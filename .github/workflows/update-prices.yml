name: Update Blox Fruits Prices

on:
  schedule:
    # 매일 오전 9시 (UTC 00:00 = KST 09:00)
    - cron: '0 0 * * *'
    # 매일 오후 9시 (UTC 12:00 = KST 21:00)
    - cron: '0 12 * * *'
  workflow_dispatch: # 수동 실행도 가능하도록

jobs:
  update-prices:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install playwright requests
          playwright install chromium
          
      - name: Run price update script
        run: |
          python scripts/update_prices.py
          
      - name: Commit and push if changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add data/games/bloxfruits.json
          git diff --staged --quiet || git commit -m "🤖 Auto-update: Blox Fruits prices ($(date +'%Y-%m-%d %H:%M'))"
          git push
